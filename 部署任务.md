# yt-dl-action · 部署任务

## 当前状态

项目已创建在本地，代码已 git commit，等待推送到 GitHub。

```
C:\Users\Administrator\Desktop\yt-dl-action\
├── .github/workflows/download.yml   ← 核心 workflow
├── playlist.txt                      ← 批量下载清单
└── README.md
```

**Git 状态**：已 `git init` + `git add -A` + `git commit`，branch 为 `main`。未设置 remote。

---

## 任务：完成 GitHub 认证 + 创建仓库 + 推送 + 测试

### Step 1：认证 GitHub CLI

```powershell
cd C:\Users\Administrator\Desktop\yt-dl-action
gh auth login --web
```

这会输出一个一次性代码（格式如 `XXXX-XXXX`）和 URL `https://github.com/login/device`。

**需要用户操作**：打开浏览器访问该 URL，输入代码，在 GitHub 上确认授权。授权完成后终端会自动继续。

如果 `gh auth login --web` 超时（等待超过 60 秒用户未操作），重试即可。

### Step 2：创建 GitHub 仓库并推送

```powershell
cd C:\Users\Administrator\Desktop\yt-dl-action
gh repo create yt-dl-action --public --source=. --push
```

这会在用户的 GitHub 账号下创建公开仓库 `yt-dl-action`，并推送本地代码。

### Step 3：验证 workflow

推送成功后，在 GitHub 网页端操作：
1. 打开 `https://github.com/<用户名>/yt-dl-action/actions`
2. 点击 **YouTube Downloader** workflow
3. 点击 **Run workflow** 下拉 → 在 URL 字段输入测试链接 → **Run workflow**
4. 等待 workflow 完成（约 1-3 分钟）
5. 在完成的 run 页面底部下载 `youtube-downloads` artifact

**测试 URL**：`https://www.youtube.com/watch?v=mpPKsA347dg`（AppleTrack 的 iOS 27 视频）

### Step 4（可选）：验证音频提取

再次手动触发，勾选 `audio_only`，确认 artifact 中是 `.mp3` 文件。

---

## workflow 说明

| 触发方式 | 操作 | 适合场景 |
|---------|------|---------|
| 手动 (workflow_dispatch) | Actions 页 → Run workflow → 填 URL | 单次下载 |
| 自动 (push) | 编辑 playlist.txt → commit → push | 批量定期 |

| 参数 | 说明 |
|------|------|
| `url` | YouTube 视频链接（手动模式） |
| `audio_only` | 只提取 MP3 音频（不下载视频） |

产物保留 **7 天**，从 GitHub Actions Artifacts 下载。

---

## 注意事项

- GitHub 必须是**用户本人**操作授权（`gh auth login` 需要浏览器确认）
- 仓库名 `yt-dl-action` 可以改，但需要同步修改 `gh repo create` 命令
- 如果已有同名仓库，`gh repo create` 会报错，换一个名字即可
- `playlist.txt` 中 `#` 开头的行为注释，空行会被忽略
- GitHub Actions 免费额度：公开仓库无限，私有仓库每月 2000 分钟
